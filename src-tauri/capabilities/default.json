{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for SpecStudio",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "shell:default",
    "dialog:default",
    "store:default",
    "process:default"
  ]
}
